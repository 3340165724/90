@全体成员 大数据本次课作业：
1、hive安装配置熟练
2、在hive中创建3个表，班级、学生、成绩，对应mysql中的三个表，其中班级表创建分区（分区列为createyear 整数类型）
3、然后将三个表的数据分别导入到hive中的3个表（其中tb_class采用2019和2020的分区导入）
4、完成下列hive-sql练习：
1）统计各个地区下学生人数
     2）统计各个地区下学生人数top5，按降序排列
     3）统计各个班级下的人数，按人数降序排列
     4）统计各个学校下的人数Top3，按人数降序排列
     5）统计各个地区下学生人数，并且进行排名（额外增加一列排名列）
           row_number()排名函数，一般需要配合over()开窗限制范围
【有一种组内排序在Hive SQL不能直接实现，借助开窗函数多一个动态附加列 辅助查询】